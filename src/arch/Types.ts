
export enum L {
    PATH,
    SIZE,
    FD,
    DFD,
    FLAG,
    ATTRMODE,
    O_FLAGS,
    VADDR,
    MPROT,
    OUTPUT_BUFFER,
    PID,
    ERR,
    SIG,
    XATTR_LIST,
    F_,
    MFD, // mapped fd
    UID,
    GID,
    UTSNAME
}

export enum T {
    INT32,
    UINT32,
    LONG,
    ULONG,
    SHORT,
    USHORT,
    FLOAT,
    DOUBLE,
    CHAR,
    STRING,
    CHAR_BUFFER,
    POINTER32,
    POINTER64,
    STRUCT
}
