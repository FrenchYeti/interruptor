
export enum L {
    PATH,
    SIZE,
    FD,
    DFD,
    FLAG,
    ATTRMODE,
    O_FLAGS,
    VADDR,
    MPROT,
    OUTPUT_BUFFER,
    PID,
    ERR,
    SIG,
    XATTR_LIST,
    F_,
    MFD, // mapped fd
    UID,
    GID,
    UTSNAME
}
